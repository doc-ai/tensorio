platform :ios, '12.0'
use_frameworks!

target 'TensorIOExamples' do

  pod 'TensorIOTensorFlow', podspec: 'https://raw.githubusercontent.com/doc-ai/tensorio-tensorflow-ios/r2.0/TensorIOTensorFlow.podspec'

  pod 'TensorIO/Core', git: 'https://github.com/doc-ai/tensorio-ios.git', branch: 'r1.2'
  pod 'TensorIO/TensorFlow', git: 'https://github.com/doc-ai/tensorio-ios.git', branch: 'r1.2'

  # pod 'TensorIO/Core', '~> 1.2.0'
  # pod 'TensorIO/TensorFlow', '~> 1.2.0'

  target 'TensorIOExamplesTests' do
    # inherit! :search_paths
    # Pods for testing
  end

end
